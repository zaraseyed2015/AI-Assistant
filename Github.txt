git remote add origin https://github.com/<username>/AI-Assistant.git

git branch -M master

git push -u origin master
--------------
git add Github.txt
git commit -m "Add Github.txt"
git push

------------------
git checkout -b feature/streaming-chat
git checkout master
git merge feature/streaming-chat
git push origin master